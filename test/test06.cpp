#include "./../lc.h"

int findPosision(int input1, int input2, int **input3) {
    vector<int> queue(input1 + 1, 1);
    int idx = 1;
    for(int i = 0; i < input2; i++) {
        int *event = input3[i];
        if(event[0] == 1) {
            while(idx <= input1 && vec)
        }
    }
}

int main(int argc, char **argv) {
    
    std::cout << "done.";
    return 0;
}